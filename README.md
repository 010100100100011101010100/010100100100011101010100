```yaml
NAME : "Shera"
```

```Generic CP Template

"#include <bits/stdc++.h>
using namespace std;
#define nline '\n'
#define ll long long


void solve(){
    int n;
      cin>>n;
      int arr[n];
      for(int i=0;i<n;i++){
        cin>>arr[i];
      }
      
      
}
int main() 
{ ios_base::sync_with_stdio(false);
      cin.tie(NULL);
      cout.tie(NULL);
    ll t;
    cin>>t;
    while(t--){
      solve();
      
     }
}
"
```


